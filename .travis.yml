language: python
python:
  - 3.6
  - 3.7
  - 3.8
  - 3.9-dev
before_install:
  - python --version
  - pip install -U pip
  - pip install -U pytest
install:
  - pip install git+https://github.com/lgarrison/asdf.git .
script: pytest
